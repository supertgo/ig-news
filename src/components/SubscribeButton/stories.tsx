import { Story, Meta } from '@storybook/react';
import SubscribeButton from '.';

export default {
  title: 'SubscribeButton',
  component: SubscribeButton
} as Meta;

export const Default: Story = () => <SubscribeButton />;
