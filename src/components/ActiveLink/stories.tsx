import { Story, Meta } from '@storybook/react';
import ActiveLink from '.';

export default {
  title: 'ActiveLink',
  component: ActiveLink
} as Meta;

export const Default: Story = () => <ActiveLink />;
